diff --git a/src/assets/css/base.css b/src/assets/css/base.css
index bf66047..5423c64 100644
--- a/src/assets/css/base.css
+++ b/src/assets/css/base.css
@@ -73,8 +73,9 @@ body { position: relative; overflow-x: hidden; }
 @keyframes aDrift4 { 0%,100%{transform:translate(0,0) scale(1);} 33%{transform:translate(-28%,-44%) scale(1.16);} 66%{transform:translate(44%,24%) scale(0.88);} } /* unused as of migration */
 @media (prefers-reduced-motion: reduce) { .aurora span { animation: none !important; } }
 /* Mobile: the blurred blob field is the dominant paint cost on throttled CPUs.
-   Hold it static (no infinite animation), thin it to 4 blobs, and drop the blur radius. */
-@media (max-width: 1000px) {
+   Hold it static (no infinite animation), thin it to 4 blobs, and drop the blur radius.
+   767px matches the deck's own mobile breakpoint (deck/lib/tokens.css). */
+@media (max-width: 767px) {
   .aurora span { animation: none !important; will-change: auto; filter: blur(52px) saturate(1.1); } /* was: filter: blur(60px) saturate(1.1); in auth.css */
   .aurora .b5, .aurora .b6, .aurora .b7, .aurora .b8 { display: none; } /* index.html only — thins the 8-blob layout to 4 on mobile; no auth.css equivalent needed since auth.css only ever had 4 blobs */
 }
diff --git a/src/assets/css/home.css b/src/assets/css/home.css
index d036b28..6c73de8 100644
--- a/src/assets/css/home.css
+++ b/src/assets/css/home.css
@@ -376,9 +376,10 @@
 }
 
 /* ---- Responsive ----
-   --edge: 40px at max-width:1000px was identical in both sources and now
-   lives in layout.css. --edge at 720px/480px below is index.html-only. */
-@media (max-width: 1000px) {
+   767px matches the deck's own mobile breakpoint (deck/lib/tokens.css),
+   so the homepage and the investor-deck overlay switch to their mobile
+   layouts at the same viewport width. --edge itself lives in layout.css. */
+@media (max-width: 767px) {
   .wcag-grid { grid-template-columns: 1fr; gap: 32px; }
   .credit-grid { grid-template-columns: 1fr; gap: 28px; }
   .feature-grid { grid-template-columns: 1fr; }
@@ -386,7 +387,6 @@
   .risk-grid { grid-template-columns: repeat(3, 1fr); max-width: 760px; }
 }
 @media (max-width: 720px) {
-  .site { --edge: 24px; }
   .site-nav, .nav { flex-direction: column; align-items: flex-start; gap: 14px; } /* .topbar intentionally excluded here — auth.css had no matching mobile stack behavior; adding it would change secondary-page rendering */
   .nav-right { flex-wrap: wrap; gap: 12px 18px; }
   .risk-title { font-size: 30px; }
@@ -400,7 +400,6 @@
   .overlay-card { flex-direction: column; gap: 14px; }
 }
 @media (max-width: 480px) {
-  .site { --edge: 16px; }
   .risk-grid { grid-template-columns: 1fr; max-width: 340px; }
   .cta-row { flex-direction: column; align-items: stretch; width: 100%; }
   .cta-top { flex-direction: column; }
diff --git a/src/assets/css/layout.css b/src/assets/css/layout.css
index 6fc502a..15182cd 100644
--- a/src/assets/css/layout.css
+++ b/src/assets/css/layout.css
@@ -23,10 +23,13 @@
  * avoid one page's rule silently overriding the other's.
  */
 
-/* ---- Shared container system ---- */
+/* ---- Shared container system ----
+   Margin values and breakpoint matched to the investor deck's own system
+   (deck/lib/tokens.css: --margin 64px, 24px at max-width:767px) so the edge
+   spacing doesn't visibly jump when the deck opens over the homepage. */
 .site, .page { --edge: max(64px, calc((100% - 1280px) / 2 + 64px)); }
-@media (max-width: 1000px) {
-  .site, .page { --edge: 40px; }
+@media (max-width: 767px) {
+  .site, .page { --edge: 24px; }
 }
 
 /* ---- Primary nav ----
